# Copy to .env and fill in, then:  set -a && source .env && set +a

# Required. All tracks call the OpenAI API.
OPENAI_API_KEY=

# Optional. If unset, W&B logging is skipped and runs still work.
WANDB_API_KEY=
